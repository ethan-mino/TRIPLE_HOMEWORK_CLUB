<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.triple.club.Api.user.mapper.UserMapper">
    <select id="findUserByUsername" resultType="com.triple.club.Api.user.vo.User">
        SELECT id, username, password, created_at, updated_at
        FROM user
        WHERE username = #{username}
    </select>

    <insert id="createUser" keyProperty="id" parameterType="com.triple.club.Api.user.vo.User">
        INSERT INTO user(id, username, password, created_at, updated_at)
        VALUES (UUID(), #{username}, #{password}, NOW(), NOW());
    </insert>
</mapper>
