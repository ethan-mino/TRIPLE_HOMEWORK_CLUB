<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.triple.club.controller.user.mapper.UserMapper">
    <select id="selectUserByUsername" resultType="com.triple.club.controller.user.vo.User">
        SELECT id, username, password, created_at, updated_at
        FROM user
        WHERE username = #{username}
    </select>

    <insert id="createUser">
        INSERT INTO user(id, username, password, created_at, updated_at)
        VALUES (UUID(), #{username}, #{password}, #{createdAt}, #{updatedAt});
    </insert>
</mapper>
